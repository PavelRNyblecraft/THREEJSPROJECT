export interface CameraPosition {
  x: number,
  y: number,
  z: number,
}

const DEFAULT_COORDINATE_VALUE: number = 10;

export const DEFAULT_CAMERA_POSITION: CameraPosition = {
  x: DEFAULT_COORDINATE_VALUE,
  y: DEFAULT_COORDINATE_VALUE,
  z: DEFAULT_COORDINATE_VALUE
}

